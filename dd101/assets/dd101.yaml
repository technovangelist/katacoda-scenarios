---
- hosts: webservers
  roles:
    - { role: Datadog.datadog, become: yes, retries: 10 }
  vars:
    datadog_api_key: "{{ lookup('env', 'DD_API_KEY')}}"
     

- hosts: webservers
  remote_user: root
  tasks:
    - name: install apache
      apt:
        name: apache2
    